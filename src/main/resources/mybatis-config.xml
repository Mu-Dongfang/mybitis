<?xml version="1.0" encoding="UTF-8" ?>
<!--核心配置文件的约束信息-->
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!--环境配置-->
<configuration>
    <!--加载类路径下的属性文件-->
    <properties resource="db.properties"/>
    <settings>
        <!--是否开启缓存，默认为ture-->
        <setting name="cacheEnabled" value="true"/>
        <!--
        延迟加载的全局开关
        开启后所有关联对象都会延迟加载
        默认为false
        -->
        <setting name="lazyLoadingEnabled" value="true"/>
        <!--
        关联对象属性的延迟加载开关
        开启后对任意延迟属性的调用都会使用带有延迟加载属性的对象向完整加载，否则每种属性都按需加载
        默认为false
        -->
        <setting name="aggressiveLazyLoading" value="true"/>
        <!--驼峰(Camel Case)命名规则映射，默认为false-->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <!--类设置别名-->
    <typeAliases>
        <!--按包扫描，自动配置别名为类名，大小写不区分-->
        <package name="com.icey.pojo"/>
    </typeAliases>
    <!--默认采用的环境，可以在编程时通过SqlSessionFactoryBuilder().build()函数覆盖-->
    <environments default="development">
        <!--配置的其中一套环境·-->
        <environment id="development">
            <!--配置事务管理器，JDBC或MANAGED-->
            <transactionManager type="JDBC"/>
            <!--
                配置数据源
                UNPOOLED:表示数据源为无连接池类型，配置后程序每次被请求时会打开和关闭数据库连接
                POOLED:表示数据源为连接池类型，比较流行
                JNDI:表示数据源可以在EJB或应用服务器等容器内使用
            -->
            <dataSource type="POOLED">
                <!--数据库连接相关配置，db.properties文件中的相关内容-->
                <!--数据库驱动-->
                <property name="driver" value="${mysql.driver}"/>
                <!--连接数据库的url-->
                <property name="url" value="${mysql.url}"/>
                <!--连接数据库的用户名-->
                <property name="username" value="${mysql.username}"/>
                <!--连接数据库的密码-->
                <property name="password" value="${mysql.password}"/>
            </dataSource>
        </environment>
    </environments>
    <!--mapper文件路径配置-->
    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
    </mappers>
</configuration>